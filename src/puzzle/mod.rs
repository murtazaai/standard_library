use log::{info, warn};

#[allow(dead_code,unused_doc_comments)]
fn solve() {
    /// Fill in the missing code
    /// How much time does it take to solve the puzzle
    /// We've got the code already
    /// Who are you?
    let a = true;
    let b = true;
    warn!("Species recognition: {}", a);
    info!("Alien detected: {}", b)
}